# Architecture Decision Records — LatentMesh

Numbered sequentially; immutable once Accepted. Status lifecycle: `Proposed` →
`Accepted` → `Implemented` / `Superseded`. This is a **research prototype**
(see the root README's Honest Status) — most ADRs below are design contracts,
not shipped systems; each states plainly what is and isn't built.

| ADR | Title | Status |
|---|---|---|
| [001](001-latentmesh-architecture-and-prior-art.md) | LatentMesh architecture and prior art — the north star, and an honest map of what LatentMAS, AVP, AAFLOW+, StateBridge, DMoA, Cisco Cognitive Fabric, and LCGuard already cover vs. the actual whitespace (causally-verified, governed, self-evolving integration) | Proposed |
| [002](002-latent-packet-protocol.md) | Latent packet protocol (`LatentFrame`) — a reference wire type for this workspace's own crates, deliberately scoped narrower than AVP (which already specifies KV/hidden-state transport); a bridge to AVP is the likely long-run wire format | Proposed (packet crate implemented) |
| [003](003-causal-edge-verification.md) | Causal edge verification — the core differentiator: an agent-to-agent latent edge is admitted to the topology only after surviving the four-control test (zero / random / mismatched / self-generated state) with statistically significant incremental value | Proposed |
| [004](004-streaming-latent-state.md) | Streaming latent state — incremental latent frames over MidStream instead of waiting for completion; downstream agents consume partial cognitive state | Proposed |
| [005](005-persistent-latent-memory.md) | Persistent latent memory — RuVector stores a continuum from raw latent trajectory to compressed trajectory to semantic prototype to symbolic rule | Proposed |
| [006](006-self-evolving-topology.md) | Self-evolving topology — MetaHarness Darwin mutates the communication graph itself, fitness-scored by causal edge value (ADR-003), not token traffic or superficial accuracy | Proposed |
| [007](007-federated-world-models.md) | Federated world models — Radio exchanges structured, compatibility-checked transition rules between nodes instead of pooling raw experience | Proposed |
| [008](008-capability-governed-execution.md) | Capability-governed execution — RVF/RVM enforce `execute(z) ⟺ signature(z) ∧ authority(z) ∧ provenance(z) ∧ risk(z) < τ`; latent execution is a governed capability, not an implicit trust | Proposed (gate implemented) |
| [009](009-online-causal-control-loop.md) | The online causal control loop — corrects ADR-001's novelty claim against a second literature pass (E2 Explainer, MANTA, BANDMAS); `G_t=(A,E,Z,M,P)`; the closed loop (execute→audit→measure→persist→evolve); a named role for every existing ruvnet component (RuFlo/MetaHarness/RuVector/MidStream/Radio/RVF+RVM/RuView/Autogenous); the one-vertical-experiment plan; ablation-based acceptance test | Proposed |
| [010](010-latentmesh-air-protocol.md) | LatentMesh Air protocol — bounded 16 through 256 byte physical frame, optional LMS1 authentication envelope, deterministic LMAD state delta, fragmentation, replay defense, and cross-language golden vectors | Accepted and partially implemented |
| [011](011-radio-adapters-and-legal-boundary.md) | Radio adapters and legal boundary — separates WiFi and BLE transports from licensed external HF, VHF, AM, FM, TNC, SDR, antenna, and operator responsibilities | Accepted and partially implemented |
| [012](012-neural-receiver-fallback.md) | Neural receiver with deterministic fallback — bounded likelihood correction subordinate to conventional synchronization, FEC, CRC, replay, authentication, and state checks | Accepted and minimally implemented |
| [013](013-esp32-firmware.md) | ESP32 firmware — native WiFi and BLE, KISS and I2S bridges, bounded queues, metrics, and default-deny external transmit policy | Accepted and implemented without hardware validation |
| [014](014-benchmark-and-acceptance-method.md) | Benchmark and acceptance — separates the tenfold semantic reduction gate from the additional twofold neural physical-layer gate and requires evidence labels | Accepted as validation contract; over-the-air gate not passed |
| [015](015-live-midstream-latent-streaming.md) | Live MidStream latent streaming — implements ADR-004: `latentmesh-stream` with bounded length-prefixed `LatentFrame` framing, gap/reorder detection, confidence-gated authority escalation, and a `midstreamer-quic 0.3` transport behind a feature; shared golden fixture keeps the MidStream-side codec byte-compatible | Accepted and implemented (live LLM deployment external) |
| [016](016-ruvector-persistent-latent-memory.md) | RuVector persistent latent memory — implements ADR-005: `latentmesh-memory` with the `LatentMemory` trait, explicit fidelity continuum re-encoded through the real quantizers with measured error, causal-value admission to the Raw tier, prototype/rule promotion, and a `ruvector-core` HNSW backend behind a feature | Accepted and implemented (remote RuVector deployment external) |
| [017](017-radio-federated-world-models.md) | Radio federation of world models — implements ADR-007: `latentmesh-federation` with bounded `TransitionRule`s carried in LMS1 envelopes over the Air stack, decoy-controlled local-scope validation reusing the gate's permutation test, structural privacy scoping, and selective transmission | Accepted and implemented (deterministic simulation; no over-the-air claim) |
| [018](018-metaharness-darwin-topology-loop.md) | MetaHarness Darwin topology loop — implements ADR-006: `latentmesh-evolve` with the causal-fitness objective, authority-never-expands mutation applier, deterministic seeded evolution, procedural-memory warm starts, and evidence-labelled JSON receipts verified by a `harness/evolve` suite | Accepted and implemented (deterministic simulation; no live-agent claim) |
| [019](019-meshtastic-transport-adapter.md) | Meshtastic transport adapter — a new `WireProfile::Meshtastic` row under ADR-011's adapter boundary (not a new PHY), a new `latentmesh-meshtastic` crate speaking Meshtastic's device-API framing, and a fixed 233-byte `frame_mtu` (217 usable Air-payload bytes per packet) since Meshtastic does not auto-fragment application payloads | Proposed |
| [020](020-agentbbs-store-and-forward-bridge.md) | agentbbs store-and-forward bridge — a decode/re-encode bridge (not a tunnel) from LatentMesh Air's LMS1/LMAD deltas onto `ruvnet/agentbbs`'s `post_message`/`FederationPayload` contract, agentbbs kept out of the Cargo dependency graph, loopback-testable against `LoopbackTransport` and the `agentbbs mcp` stdio binary | Proposed |
| [021](021-cognitum-one-api-integration.md) | cognitum-one API integration — two distinct surfaces (cloud fleet `api.cognitum.one` `/v1/seed/*`+`/v1/spaces/*` vs. local V0 appliance `/api/v1/v0/*`), a feature-gated `latentmesh-cognitum-client` with Ed25519 canonical-request signing in the default build and HTTP transport behind a feature, ADR-099's `/v1/spaces/{siteId}` envelope as the design reference for LMAD's missing promotion-gate schema | Proposed |
| [022](022-self-optimizing-metaharness-e2e-loop.md) | Self-optimizing MetaHarness e2e loop — extends `harness/` with a `harness/integration/` suite covering the three new integrations end-to-end in loopback, optional subprocess-invoked MetaHarness score/genome/mcp-scan/threat-model gates that never become a workspace build dependency, and a Darwin-loop-style optimizer over adapter parameters (MTU packing, fragmentation threshold, bridge batching) | Proposed |
| [023](023-live-four-condition-run1-pre-registration.md) | Live latent-exchange experiment, run 1 — pre-registration implementing ADR-009 §5's one-vertical-experiment plan (four conditions, injection semantics, A1-A8 criteria, dataset pins and split seeds, deviations ledger) followed by the S6 results record: mechanics transmit (S1a p=0.03125) and the affine cross-model alignment fits well (held-out residual 0.51 gold / 0.45 generated), but the aligned vector is statistically indistinguishable from random noise at both registered depth cells under both calibration distributions (A7(b) FAIL ×4) — a clean negative result; S3-S6's A1-A8 were never computed, per the pre-committed stop rule | Concluded — negative result, pre-registration honored |
| [024](024-run2-trained-thought-adapter-ladder.md) | Run 2: trained thought-adapter ladder — response to run 1's negative result via a new `latentmesh-train` crate (workspace-excluded like `latentmesh-runtime`, path-dependent on it): per-token capture from already-preserved S2c token streams (no regeneration), a leakage-exclusion rule for the 13 probe/training-overlap rows, and an ordered ladder (M3 MLP projector, M4 FastGRNN sequence translator, M5 receiver-side MicroLoRA from ΔV feedback, M6 the original A1-A8 finally computed) — each rung gated on ADR-023's frozen 40-item probe, reused unchanged | Proposed (M1 — plan milestone; M0 bootstrap scouting done, M2-M6 not started) |
| [025](025-distributed-latent-data-fabric.md) | Distributed latent-data fabric — adopts `ruvector-replication` 0.1.1 (multi-master, quorum writes, vector-clock/CRDT conflict resolution, Rust 1.77+ — MSRV-compatible with this workspace unlike candle) as the designated replication layer for ADR-016's memory continuum and run-2's dataset shards/receipts, with per-artifact consistency guidance and gate-verified causal edges explicitly excluded (that's ADR-026's contract) | Proposed — design contract, no code |
| [026](026-verified-edge-federation-wire-contract.md) | Verified-edge federation wire contract — adopts agentdb's `CausalEdgeSync` QUIC message shape as reference prior art, adapted to carry ADR-003's `ΔV`/`worst_p_value`/decoy-control provenance/authority ceiling/signature; only edges that passed the five-control test may publish, receivers always re-verify locally before granting authority (federated claims are hints, never authority, per ADR-008 and the cognitum ADR-099 self-attestation precedent already cited in ADR-021); transport-agnostic, ~140 bytes, fits one Air frame unfragmented | Proposed — design contract, no code |
| [027](027-latent-prefix-context-window-delivery.md) | Latent-prefix context-window delivery (the M4.5 contingency) — registers soft-prefix conditioning at the receiver's input boundary as the pre-named fallback if run 2's mid-layer injection gates (M3/M4) fail, mapped to `latentmesh-gate`'s existing `LatentPrefix` authority tier; a strictly easier transfer problem than mid-layer overwrite (usefulness-as-input, not valid mid-layer geometry) but explicitly NOT part of run 2's current scope and requires its own ADR-023-style pre-registration before any probe | Proposed — registration only, not scheduled |
| [028](028-evolutionary-adapter-search-anti-gaming.md) | Evolutionary adapter search (Darwin/AVO/flywheel) with anti-reward-hacking guardrails — a strict two-list separation (adapter architecture/depth/pooling/loss/rank are evolvable, fitness computed on `adaptation-512` only; the frozen 40-item probe, eval/holdout locks, ADR-003's five controls, and the pre-registration rules themselves are never evolvable), a one-champion-per-rung promotion rule spending exactly one frozen-probe draw, `metaharness_reward_hack_scan`+`metaharness_replay_verify` as mandatory but explicitly insufficient gates, AVO scoped to the engineering lane only, and the flywheel's verified-empty genesis state plus its Ed25519 key-management prerequisite named up front | Proposed — design contract, no code |
| [029](029-autogenous-governed-promotion-adoption.md) | Adopting autogenous's governed-promotion contract — ADOPTS (design-level) autogenous's fitness-vector AND-gate promotion mapped onto ADR-003's ΔV, type-level authority-never-expands as a goal for `Authority`, expiring/rollback-able Antibody-Package shape as the reference to close ADR-026's TTL/containment gap, and ≥2-pinned-judge signed promotion as a concrete instantiation of the hierarchical-vouching design 027 sketched; CITES its measured performance and AGL schema as prior art, and radio-moe's fusion result with a corrected regime-dependent framing (governance-attributable, not a raw 33.3% claim); REJECTS taking `autogenous` as a Cargo dependency or wholesale-replacing `Authority`'s runtime checks — names an unresolved attribution discrepancy between autogenous's README and `latentmesh-gate`'s own header | Proposed — design contract, no code |
| [030](030-run3-causally-gated-text-pre-registration.md) | Run 3 pre-registration: causally-gated TEXT communication — tests whether ADR-003's five-control gate has standalone value on the text channel agents already use, independent of whether latent transfer works; three conditions (`StaticText`/`StaticText+Gate`/`CausalDynamicText`), four controls (`text_equivalent` dropped as degenerate for a text channel, justified explicitly), reuses ADR-023's frozen 40-item probe under a new, separately-justified pre-registration, pre-registers both the 40-item (~0.26 GPU-h) and eval-200 (~1.3 GPU-h) scales together, and is the run that finally satisfies ADR-023's genome-freeze requirement — unlocking `eval-200`/`holdout-100` with provenance recorded in the receipt itself | Proposed (pre-registration) |
| [031](031-evidence-receipt-and-statistical-protocol-governance.md) | Evidence, receipt, and statistical-protocol governance — formalizes the receipt required-field contract, naming convention, and append-only rule already de-facto followed across 60+ committed receipts; states six standing statistical-protocol rules extracted from ADR-023/024/028 practice (pre-registration before any outcome is known, frozen protocols never iterated, one registered draw per rung, honest-fail paths named before the outcome, superseded runs preserved and disclosed, protocol-safe annotation vs. a probe draw); reserves an explicit, unresolved slot for the in-flight `docs/research/031` statistical-power/sequential-design pass rather than pre-empting its findings | Proposed — design contract, no code |
| [032](032-negative-result-publication-contract.md) | Negative-result publication contract — states what makes a null PUBLISHABLE (pre-registered before the outcome, mechanics/integrity gates passed, adversarially reproduced, confounds named and scoped) versus merely recorded or inconclusive, what a negative-result artifact must contain (the precise claim not supported, a closed attribution chain, registered confounds, full receipts), and the rule that a null is never re-run under a changed protocol to convert it into a pass — modeled on ADR-023 §S6 and `docs/research/025`, checked against all four of this repo's adversarially-verified nulls (run-1 gold ×2, run-1 generated ×2, M3 ×2, M4 ×3) | Proposed — design contract, no code |
| [033](033-vendored-dependency-and-upstream-contribution-policy.md) | Vendored-dependency and upstream-contribution policy — states when vendoring a third-party file (not a dependency graph) is permitted, the deviation-ledger requirement every vendored file must carry (provenance block, numbered deviations classified numerically-inert vs. output-changing, uniform-application evidence for the latter), the version-pinning rule (copy the vendoring crate's `Cargo.lock` verbatim, already practiced by `latentmesh-train`), and the upstream-contribution obligation this repo has not yet met for the genuine BF16 RoPE aliasing bug fix found in vendored `candle-transformers` 0.9.2 — explicitly flags that whether the bug persists in `candle` main is UNVERIFIED | Proposed — design contract, no code |
| [034](034-concurrent-lane-resource-scheduling.md) | Concurrent-lane resource scheduling — formalizes the mission's three-lane split (implementation/GPU, analysis/CPU, research/network) against a single RTX 5080, already practiced informally via `.ruvnet-brain/checkpoint.json`'s `lanes` field: exactly one GPU-holding workflow at a time named in the checkpoint, CPU-only lanes declare scope in their brief, a lane needing the GPU queues rather than preempts, long-running captures checkpoint incrementally (the S2c resume that saved ≈1 GPU-h is the worked example), artifacts live in gitignored `target/` with receipts committed, and every workspace-excluded crate gets its own `.gitignore` entry at creation — the standing rule the M3 near-miss (7,071 `target/` artifacts almost committed) exists to prevent a recurrence of | Proposed — partially already practiced, no new code |
| [035](035-m4b-scale-control-pre-registration.md) | M4b scale-control arm pre-registration — the ladder's one mandatory-regardless-of-outcome contingency (arXiv:2608.05164's ~1.7B receiver threshold, corroborated by Bicameral's own GSM8K degradation at small capability gaps): a Qwen2.5-3B self-pair (both 7B-cross-architecture options ruled out on VRAM/precedent grounds, the self-pair's PASS/NULL asymmetry registered explicitly), fresh receiver-side capture/mechanics/baselines (the frozen probe's controls must be re-measured under the new receiver, never compared against the 1.5B-receiver's numbers), a reconstruction-trained M3-shaped adapter with an M4g-conditional injection operator, mid-p McNemar primary with the e-process registered as optional not mandatory (amended by ADR-036 — reversed to e-process primary), and receipted-rate cost estimates (~1-2 GPU-h) | Proposed (pre-registration) |
| [036](036-successor-rung-evaluation-protocol.md) | Successor-rung evaluation protocol — replaces the frozen 40-item sign test for run-2 rungs not yet drawn (M4i, M4h Stage 2, M4b, and later), since discordant-pair counts collapsed as adapters improved (M4d 7 → M4g 3 → M4h-S1 2, the ladder's best-behaved payload yet the least measurable); adopts ADR-030's anytime-valid e-process verbatim, drawing its item stream entirely from `adaptation-512` after correcting a discovered error — the frozen 40-item probe is NOT a subset of `adaptation-512` (verified: 1-item overlap, not 40) — and states explicitly that completed rungs are never re-drawn under the new protocol and the two eras' results are never conflated, per ADR-032's no-protocol-shopping rule | Proposed (pre-registration) |
| [037](037-m5x-maximal-configuration-rung.md) | M5X: the multi-factor "maximal configuration" rung — deliberately breaks the ladder's one-factor-per-rung discipline to test whether transfer requires a *conjunction* of properties (per C2C's own Table 10: single-layer content injection reproduces LatentMesh's own null, ~0.1pp over baseline, while every working method combines multi-layer + continuous delivery + task-loss training); combines multi-layer injection (L18/L24→L14/L19, zero new capture needed), de-pooling (M4h Stage 1's weights), fuse delivery (M4g's operator) and task-loss training, explicitly excluding M4i's ordinary-token axis and continuous injection with reasons stated; rules 4+4 slots (total 8) for this rung only without adjudicating ADR-028's general slot-count contradiction; registers in advance both what a PASS licenses (transfer is achievable, not which factor mattered) and the unsparing reading of a NULL (the strongest negative this project can produce, leaving only M4b's receiver-scale arm untested) | Proposed (pre-registration) |
